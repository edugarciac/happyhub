## 1. Fix CSP

- [x] 1.1 Widen `connect-src` in `next.config.js` to `https://*.google-analytics.com https://*.analytics.google.com`

## 2. Verify

- [x] 2.1 Confirm `next build` still succeeds
- [ ] 2.2 Deploy and confirm GA4 Realtime shows a user after accepting the cookie banner (MANUAL)
