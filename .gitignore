# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# AWS credentials (NEVER commit these)
.aws/
aws-credentials*.json
aws-credentials*.local
aws-config*.local
aws-config*.json
**/aws-credentials*
**/aws-config*

# Airtable credentials (NEVER commit these)
airtable-credentials.json
**/airtable-credentials*

# Migration data (NEVER commit these)
migration/airtable-export/
migration/backups/
migration/*.csv
migration/*.sql.backup
migration/*.dump
migration/.db-password
migration/connection-string.txt
migration/aurora-endpoint.txt
migration/aurora-sg-id.txt

# Amplify deployment files with credentials
amplify-env-variables.txt
*-env-variables.txt

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
aws-credentials-happyhub.json

# GitHub Spec Kit (may contain credentials/tokens)
.claude/
.speckit/

# Style Dictionary
/build
/tokens
AMPLIFY_ENV_VARS_GOOGLE.txt
docs/GOOGLE_OAUTH_CREDENTIALS.md
allianz-claude-code-helper-darwin-arm64
.worktrees/
